#include<bits/stdc++.h>
using namespace std;
//P1601 ¸ß¾«¶È
const int maxn=510;
char a[maxn];
char b[maxn];
char sum[maxn];


int main()
{
	gets(a);
	gets(b);
	memset(sum,0,strlen(sum));
	int x,y;
	x=strlen(a);
	y=strlen(b);
	
	return 0;
}
